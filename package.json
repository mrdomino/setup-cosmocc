{
  "name": "setup-cosmocc",
  "version": "1.0.0",
  "description": "GitHub Action to setup the cosmocc toolchain",
  "main": "dist/index.js",
  "scripts": {
    "build": "bun build src/main.ts --outfile=dist/index.js --target=node --minify",
    "test": "bun test",
    "dev": "bun run src/main.ts",
    "clean": "rm -rf dist"
  },
  "license": "ISC",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/tool-cache": "^2.0.2"
  }
}
